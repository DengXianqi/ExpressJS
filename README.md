What is JSON?

JSON stands for JavaScript Object Notation.
It’s a lightweight data format used to store and exchange information between systems — especially between a server (like your Express app) and a client (like a web browser).

It looks like JavaScript objects, but it’s really just text — a string that can be easily sent over the internet.
